<UserControl
    x:Class="UITests.Shared.Windows_UI_Xaml_Controls.WebView.WebView_NavigateToLongString"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UITests.Shared.Windows_UI_Xaml_Controls.WebView"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

  <Grid>
	<Grid.RowDefinitions>
	  <RowDefinition Height="Auto" />
	  <RowDefinition Height="*" />
	</Grid.RowDefinitions>

	<StackPanel Background="Honeydew">
	  <TextBox Header="Size of (big) string, in thousands of lines" Text="1000" x:Name="WebView_NavigateToStringSize" InputScope="Number" />
	  <Button Content="generate long string" Click="GenerateLong_Click" x:Name="startButton" />
	</StackPanel>

	<WebView Grid.Row="1" Name="webViewControl" />
  </Grid>
</UserControl>
